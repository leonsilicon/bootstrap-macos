import { createBootstrapper } from '~/utils/bootstrapper.js';

export const javaBootstrapper = createBootstrapper({
	name: 'Java',
	todo: true,
	bootstrap() {
		// todo
	},
});

export default javaBootstrapper;
