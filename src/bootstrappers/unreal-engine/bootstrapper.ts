import { createBootstrapper } from '~/utils/bootstrapper.js';

export const unrealEngineBootstrapper = createBootstrapper({
	name: 'Unreal Engine',
	todo: true,
	bootstrap() {
		// todo
	},
});

export default unrealEngineBootstrapper;
