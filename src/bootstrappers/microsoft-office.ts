import { createBootstrapper } from '~/utils/bootstrapper.js';

export const microsoftOfficeBootstrapper = createBootstrapper({
	name: 'Microsoft Office',
	bootstrap() {
		// todo
	},
});

export default microsoftOfficeBootstrapper;
