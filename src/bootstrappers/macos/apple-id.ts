import { createBootstrapper } from '~/utils/bootstrapper.js';

export const appleIdBootstrapper = createBootstrapper({
	name: 'Apple ID',
	bootstrap() {
		// todo
	},
});

export default appleIdBootstrapper;
